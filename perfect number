#include <stdio.h>
#include<math.h>
int main() {
    int i,n,c=0;
    scanf("%d",&n);
    for(i=1;i<n;i++)
    {
    if(n%i==0)
    c=c+i;
    }
    if(c==2)
    printf("perfect no");
    else
    printf("not a perfect number");

     return 0;
}
